interface:
  display_name: "Unix Research Workflow"
  short_description: "Manage local ML experiments with intents, metrics, and reports"
  default_prompt: "Use $unix-research-workflow to create, inspect, or summarize an experiment in this repository."

policy:
  allow_implicit_invocation: true
